@model IEnumerable<AssistanceManager.Assistance>

@{
    ViewBag.Title = "Pedidos de Assistência";
}

<h2>Pedidos de Assistência</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DateTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Report)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customers.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Priority1.Priority1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssitanceStatus.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DateTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Report)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customers.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Priority1.Priority1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssitanceStatus.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssistanceID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssistanceID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssistanceID })
        </td>
    </tr>
}

</table>
